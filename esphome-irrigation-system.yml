esphome:
  name: esphome-irrigation-system
  friendly_name: Esphome Irrigation System

esp32:
  variant: esp32
  framework:
    type: esp-idf

preferences:
  flash_write_interval: 15sec

logger:
  level: DEBUG
  baud_rate: 115200

# Enable Home Assistant API
api:
  reboot_timeout: 15min

ota:
  platform: esphome

# Multicast DNS. Allows the device to be discovered on the local network by its hostname 
# (e.g., esphome-air-purifier.local)
mdns:
  disabled: false

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none

  manual_ip:
    static_ip: 192.168.7.190
    gateway: 192.168.7.1
    subnet: 255.255.255.0

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Esphome Irrigation System"
    password: "irrigationsystem123"

captive_portal:

switch:
  - platform: gpio
    pin: GPIO26
    name: "Pump 1"
    id: pump_1
    on_turn_on: 
      then:
        - if:
            condition:
              - binary_sensor.is_off: water_level_ok
            then:
              - delay: 1s
              - switch.turn_off: pump_1

  - platform: gpio
    pin: GPIO33
    name: "Pump 2"
    id: pump_2
    on_turn_on: 
      then:
        - if:
            condition:
              - binary_sensor.is_off: water_level_ok
            then:
              - delay: 1s
              - switch.turn_off: pump_2

button:
  - platform: template
    name: Start Watering
    id: button_start
    icon: "mdi:power-on"
    on_press:
      then:
        - if:
            condition:
              - binary_sensor.is_on: water_level_ok
            then:
              - switch.turn_on: pump_1
              - delay: !lambda "return id(watering_duration_1).state * 1000;"
              - switch.turn_off: pump_1
              - if:
                  condition:
                    - binary_sensor.is_on: water_level_ok
                  then:
                    - switch.turn_on: pump_2
                    - delay: !lambda "return id(watering_duration_2).state * 1000;"
                    - switch.turn_off: pump_2
                  else:
                    - switch.turn_off: pump_2
            else:
              - switch.turn_off: pump_1
              - switch.turn_off: pump_2

  - platform: template
    name: Stop Watering
    id: button_stop
    icon: "mdi:power-off"
    on_press:
      then:
        - switch.turn_off: pump_1
        - switch.turn_off: pump_2
      
number:
  - platform: template
    name: Full Distance
    icon: mdi:arrow-collapse-vertical
    entity_category: config
    id: full_distance_m
    min_value: 0.01
    max_value: 1.50
    initial_value: 0.1
    optimistic: true
    step: 0.01
    restore_value: true
    unit_of_measurement: meters
    mode: box

  - platform: template
    name: Empty Distance
    icon: mdi:arrow-expand-vertical
    entity_category: config
    id: empty_distance_m
    min_value: 0.01
    max_value: 1.50
    initial_value: 0.1
    optimistic: true
    step: 0.01
    restore_value: true
    unit_of_measurement: meters
    mode: box

  - platform: template
    name: Safe Level Distance
    icon: mdi:arrow-expand-vertical
    entity_category: config
    id: safe_distance_m
    min_value: 0.01
    max_value: 1.50
    initial_value: 0.1
    optimistic: true
    step: 0.01
    restore_value: true
    unit_of_measurement: meters
    mode: box

  - platform: template
    name: Watering Duration 1
    icon: mdi:clock-edit
    id: watering_duration_1
    min_value: 1
    max_value: 3600
    initial_value: 10
    optimistic: true
    step: 1
    restore_value: true
    unit_of_measurement: seconds
    mode: box

  - platform: template
    name: Watering Duration 2
    icon: mdi:clock-edit
    id: watering_duration_2
    min_value: 1
    max_value: 3600
    initial_value: 5
    optimistic: true
    step: 1
    restore_value: true
    unit_of_measurement: seconds
    mode: box

binary_sensor:
  - platform: template
    name: "Water Level OK"
    id: water_level_ok
    lambda: |-
      if (id(ultrasonic_sensor_distance).state < id(safe_distance_m).state) {
        //there is enough water
        return true;
      } else {
        //there is not enough water
        return false;
      }
    on_state:
      then:
        - lambda: |-
            if (x == false) {
              id(pump_1).turn_off();
              id(pump_2).turn_off();
            }
        
  - platform: template
    name: "Pump 1 Status"
    id: pump_status_1
    device_class: running
    lambda: |-
      return id(pump_1).state;

  - platform: template
    name: "Pump 2 Status"
    id: pump_status_2
    device_class: running
    lambda: |-
      return id(pump_2).state;

sensor:
  - platform: ultrasonic
    trigger_pin: GPIO22
    echo_pin: GPIO21
    name: "Level Sensor Distance"
    id: ultrasonic_sensor_distance
    device_class: distance
    update_interval: 1s
    pulse_time: 10us
    timeout: 20m
    filters:
      - sliding_window_moving_average:
          window_size: 15
          send_every: 15

  - platform: template
    name: "Water Level"
    id: water_level
    lambda: |-
        return id(ultrasonic_sensor_distance).state;
    update_interval: 1s
    accuracy_decimals: 0
    unit_of_measurement: "%"
    filters:
      - lambda: return ((100 - 0)/(id(full_distance_m).state - id(empty_distance_m).state)) * x + (100 - (((100 - 0)/(id(full_distance_m).state - id(empty_distance_m).state)) * id(full_distance_m).state));
      - clamp:
          min_value: 0
          max_value: 100
          ignore_out_of_range: false
